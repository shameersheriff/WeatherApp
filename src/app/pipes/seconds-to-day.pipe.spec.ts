import { SecondsToDayPipe } from './seconds-to-day.pipe';

describe('SecondsToDayPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToDayPipe();
    expect(pipe).toBeTruthy();
  });
});
