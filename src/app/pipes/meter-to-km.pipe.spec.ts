import { MeterToKMPipe } from './meter-to-km.pipe';

describe('MeterToKMPipe', () => {
  it('create an instance', () => {
    const pipe = new MeterToKMPipe();
    expect(pipe).toBeTruthy();
  });
});
